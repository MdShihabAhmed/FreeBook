<p>
  <%= comment.body %>
  <small>by: <%= link_to comment.user.username, user_path(comment.user) %> at: <%= comment.created_at.in_time_zone("Asia/Dhaka").strftime("%l:%M %p, %a %b %e") %></small>
</p>