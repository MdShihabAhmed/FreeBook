<h1>Feed</h1>

<% @posts.each do |post| %>
  <li><%= link_to post.user.full_name, user_path(post.user) %>
    posted: <%= link_to post.title, post_path(post) %>
    at: <%= post.created_at.in_time_zone("Asia/Dhaka").strftime("%l:%M %p, %a %b %e") %>
  </li>
<% end %>